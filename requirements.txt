# ── Core ──────────────────────────────────────
ultralytics>=8.2.0
opencv-python>=4.9.0
numpy>=1.26.0

# ── Offline text-to-speech (audio guidance) ───
pyttsx3>=2.90

# ── PyTorch with CUDA 12.x (RTX 3050) ────────
# Install separately via the PyTorch install selector:
#   https://pytorch.org/get-started/locally/
# Recommended command (CUDA 12.1):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
